import React from 'react';
import { Typography, Button } from '@material-ui/core';

const EVSystem = () => {
  return (
    <div>
      <Typography variant="h5">EV운영시스템</Typography>
      <Typography variant="body1">Cloud서비스</Typography>
      <Button variant="contained" color="primary">바로가기</Button>
    </div>
  );
};

export default EVSystem;
